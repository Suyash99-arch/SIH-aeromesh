# AeroMesh Frontend Environment Configuration (.env.example)
# In production on Vercel, leave empty or set to /api for relative same-origin routing.
# In local development, set to http://localhost:8000/api or leave empty for Vite proxy.
VITE_API_URL=/api
VITE_API_BASE_URL=/api
